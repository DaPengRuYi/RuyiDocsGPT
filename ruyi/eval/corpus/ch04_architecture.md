# 第 4 章　架构拆解：前端、后端、队列、数据库、向量库

> 状态：**占位骨架**（大纲已定，正文待写）
> 本章你会亲手做出：标注源码路径的架构图 + 模块清单表
> 主线阶段：阶段 2 跑通如意知识库工厂　｜　难度：进阶
> 参考材料：Task 001《DocsGPT 架构拆解.md》（已有）

## 开篇：一个真实场景 / 痛点
> 占位：想改一个功能，却不知道代码在哪——先有地图再动手。

## 本章目标
- 读懂 DocsGPT 整体结构，知道每个模块在哪个目录、干什么；
- 为后面二开建立"地图"。

## 本章小节（9 节）
1. 顶层结构：`application` / `frontend` / `extensions` / `deployment` / `scripts`
2. 后端三进程：ASGI 主应用（Flask+Starlette）、Celery worker、MCP 服务
3. 前端：Vite + React（本书二开阶段先不深改 UI）
4. 数据库层：Postgres 存用户数据，alembic 24 个迁移是怎么回事
5. 缓存与队列：Redis 承担 broker / result / cache 三个逻辑库
6. 向量库层：`vectorstore/`——FAISS 默认，还支持 pgvector/qdrant/milvus 等
7. 核心链路目录导航：`parser/` `retriever/` `llm/` `api/answer/`
8. 配置中枢：`core/settings.py` 一个类几百个字段
9. 画出你自己的 DocsGPT 架构图（对照源码逐块确认）

## 核心概念（大白话）
> 占位：什么是"前端/后端/队列/数据库/向量库"分层，各管一段。

## 实战步骤
> 占位：对照 `D:/ai/RuyiDocsGPT` 实际目录，逐模块标注路径，画架构图。

## 关键代码 / 命令 / 配置
> 占位：目录树 + 关键入口文件清单（upload.py / worker.py / classic_rag.py / llm/providers 等）。

## 常见坑
> 占位：被"几百个配置项"吓到——先抓主链路那几块，其余按需。

## 本章小结
> 占位：太完整=太重，先看懂再决定改哪、不碰哪。

## PPT 转化提示
- DocsGPT 整体架构图（分层着色）
- "资料从上传到回答经过哪些模块"流水线图
- 模块-目录对照表

## 课堂练习
给 5 个功能（上传文件/问答/显示引用/切块/换模型），让学员说出各自落在哪个目录。

## 课后作业
写《DocsGPT 模块清单》：每模块一行——路径 + 作用 + 是否第一阶段要改。

## 章末交付物
一张标注了源码路径的架构图 + 模块清单表。
